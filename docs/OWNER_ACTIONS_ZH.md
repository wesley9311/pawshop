# PawShop 店主待办与链接清单

更新：2026-09-17（WorkBuddy 第七轮）
配套：`docs/REMAINING_WORK.md`（技术视角的未完成清单与优先级）、`docs/RUNBOOK.md`（可执行命令）、`PRODUCTION_HANDOFF_ZH.md`（路径与排错总索引）。

**这份文件的用途**：把"必须由店主本人出面"的事项集中成一份可照着点的清单——每条都给出**官方链接、逐条步骤、完成后把什么交给我、我拿到之后做什么**。
凡是**不需要你动手**的，都不在这份文件里；那部分见 §7。

图例：**只有你能做** = 涉及你的身份、账号归属、协议签署、付款；**我做** = 我直接在生产环境执行并给出证据。

---

## 0. 一页速览

| # | 事项 | 状态 | 只剩你要做的动作 |
| --- | --- | --- | --- |
| 0 | **后台账号 + 忘记密码** | ✅ 账号已建好、真实登录验证通过（**2026-09-18 发版后重新验过一次，数据没丢**）；✅ **"忘记密码"已是真功能**——发信凭据已装好并端到端实测通过（2026-09-18 14:59，§0.1） | 无（已结） |
| 1 | **告警通道**（P0） | ⚠️ **飞书 ✅ / Slack ❌**（2026-09-18 **两轮**实测：飞书告警与恢复均被接受；**Slack 两轮都回 404**）。而且你给我的那个地址**与主机上现存的逐字相同**（指纹同为 `cb1f84d459ef`）、**结构完整**（标准 `T`+10 位 / `B`+10 位 / 24 位，无截断）→ 404 的含义是"**这条 webhook 在 Slack 侧已被撤销**"，不是我们配错。现在是"1/2 通道"，告警仍能到你手上，但少了一条冗余 | 重新生成 **Slack Incoming Webhook URL**（§1.6，1 分钟） |
| 2 | **备份链**（P0） | ✅ 凭据已建、已实测、四项阻塞全清；**此后每次发版都会跑一次**备份 + 异地精确版本回读 + 隔离恢复演练（2026-09-18 已跑通） | 无 |
| 3 | OSS 生命周期三条规则 | ✅ **已配置并逐条核对**（90/365/1095 天） | 无 |
| 4 | **临时 RAM 用户清理** | ⚠️ 我删不掉，需要你点两下 | 删除 `pawshop-agent-temp`（§2.3） |
| 5 | **推送与发版** | ✅ 已全部推送；**`aaa5673` 已发版上线**——升级路径首次实跑：**147 关系 / 601 行一行未少，你的后台账号还在** | 无 |
| 6 | 收款通道 | 待你申请 | 申请空中云汇（第一候选）+ PingPong（备用） |
| 7 | 后台暴露方式（B2） | 我做（需你点头） | 看 §6 三个选项，认可推荐方案即可 |
| 8 | `main` 分支合并与 CI | 我做 | 无需动手（商务已激活，现在可以合了） |
| 9 | 正式上线前的合规/物流资料 | 待你准备 | 企业邮箱、退货地址、客服责任人、经营主体 |

> **卡在你手上的只剩两件小事**：① 重新生成 Slack webhook URL（§1.6，1 分钟，把告警从"1/2 通道"恢复到双通道）；② 删掉那个已经作废的临时用户（§2.3，1 分钟）。**授权码那件已经结掉了**——"忘记密码"现在真的能发信，凭据已装好并端到端验收通过（§0.1，2026-09-18 14:59）。**商务后台的工程侧已经全部通了**——升级路径首次实跑成功，你的后台账号和库里的数据一个字都没丢。

---

## 0.1 你的后台账号与「忘记密码」（2026-09-17 新增，需要你 2 分钟）

### 你的后台登录名已经建好了（用你的 QQ 邮箱）

- **登录名**：`504533680@qq.com`。这是仓库里 112 个提交的作者邮箱，我按你说的"用真实 QQ 邮箱"填了它。**如果不对，告诉我，30 秒改掉。**
- **密码**：我随机生成的（32 位以上），写在**服务器上只有 root 能读**的文件里（`/root/pawshop-production-owner-credentials.json`，权限 `0600`）。**它没有被打印到任何地方、也没有经过聊天记录。** 你想看就自己在服务器上执行：
  ```bash
  cat /root/pawshop-production-owner-credentials.json      # 看完记得清一下 shell 历史，或立刻改密码
  ```
  或者说一句，我教你一条命令把密码复制到剪贴板。
- **已用真实登录验证过**：我用这份凭据登进去，并把后台的商品 / 订单 / 客户接口都调通了（脚本输出 `Production owner login and authenticated product, order, and customer management access passed.`）。
- **怎么进后台**：后台只监听服务器本机（不对外暴露，这是有意的），所以要先开一条 SSH 隧道。跟我说一声，我发你一条可直接粘贴的命令；连上后浏览器打开 `http://127.0.0.1:9000/app`。

### 「忘记密码」已接通并实测通过（2026-09-18 结项）

**在这之前它是个假功能**：Medusa 只负责生成重置令牌，而它自带的通知模块**只写日志、不发邮件**——你点"忘记密码"，页面会说成功，然后什么都不会发生。我已经把整条链路补齐并逐项验证：

- 订阅者已注册：触发时服务日志明确显示 `Processing auth.password_reset (priority: 100) which has 1 subscribers`；
- 令牌真的写进了数据库（`auth_password_reset_token` 里有一行）；
- 我在服务器上用真实端点触发过一次，服务如实报告 `no email sent to … - no email relay is configured`——**它不会假装成功**（这正是今晚飞书那件事教我的）。

**2026-09-18 14:59：这一步已完成，并跑通了完整验收。** 你放在桌面 `webhook URL.rtf` 里的那串 16 位授权码已经装到主机。当时的过程一步没省，留档如下：

1. ~~开启 QQ 邮箱的 IMAP/SMTP 服务，把授权码给我~~ → ✅ **你已完成**。
2. ~~把授权码交给我~~ → ✅ **已交接**：我直接从 `webhook URL.rtf` 读取，**全程没有**把授权码打进命令行、聊天或 Git；读完本地与主机两边的临时副本都做了**覆写后删除**。
3. 安装顺序（**先证明能发信，才允许写进服务器**）：

   1. 拿这串授权码**真发一封自检邮件**到你自己的邮箱；
   2. 对方接受了，才写 `/etc/pawshop/email-credentials.json`（`root:pawshop 0640`、非符号链接，且由**跑服务的那个身份**去复核它读得到）；
   3. 立刻跑一次真实的"忘记密码"端到端验收（§0.1.1），标准是**服务日志里写着这封信已交给中继**，不是"文件写好了"。

   > 所以不会出现"配置看着好好的、其实一封都发不出去"——**错的授权码在写入之前就被拒**，服务器上不会留下一个假装的配置。这一点我用本地假中继实测过：接受则投递恰好 1 封，拒绝则明确报认证失败。
4. 从此忘记密码就能自助：后台点"忘记密码" → 重置链接发到你邮箱（**15 分钟有效、只能用一次**）。以后有了小团队也一样——每个成员用自己邮箱，都能自助找回。
   - 后台是回环地址，所以邮件里**会自动附上"先开 SSH 隧道再点链接"的说明**，免得你以为链接失效。

**实测结果（2026-09-18 14:59，逐字来自主机输出）**：

```
Probing smtp.qq.com:465 by sending one self-check message to 504533680@qq.com ...
The relay accepted the credential and the message.
Installed /etc/pawshop/email-credentials.json (root:pawshop 0640).
The commerce service identity can read it.
Password reset delivery verified: the subscriber handed the message to the relay for 504533680@qq.com.
```

你邮箱里应该收到两封（一封自检、一封重置链接）。**装进去的授权码与你在文件里写的那串逐字节相同**（`sha256` 前 12 位 `4007df34874c`），文件是 `root:pawshop 0640`，并且由**跑服务的那个身份**复核过读得到。

### 0.1.1 凭据装好后，怎么算"真的通了"

不看配置文件，只看**服务自己说的话**。我装完后会跑：

```bash
ssh <生产主机> /root/run-password-reset-verification.sh delivered
```

它会**真的触发一次密码重置**，然后从服务日志里取出这次请求的结果，逐字比对，只有出现

```
password reset: reset email delivered to 504533680@qq.com
```

才算通过（否则打印出服务实际的拒收原因并失败退出）。判断标准设计成**不可自欺**：期望值要我显式声明，而且必须与主机上凭据文件的实际状态一致，否则直接拒绝评估——免得"看到发生了什么，再把它写成预期"。

- 凭据文件是**每次请求都重新读**的，所以装完**不需要重启服务**。这点我用真实触发验证过：装上文件、不重启、立刻触发 → 服务就换了行为；删掉文件、仍不重启 → 立刻退回"没有发信通道"。服务的启动时间与重启计数全程未变。
- 授权码如果填错了，服务**不会假装成功**，而是逐字报出拒收原因（例如认证被拒的 SMTP 码），我在日志里就能看到。
- 想完全绕开我、自己动手也行：在生产主机上以 root 跑 `/root/pawshop-set-email-credentials.mjs --code-file <存授权码的文件>`，它会走上面同一套顺序（自检 → 写入 → 复核 → 端到端）。

**在你给授权码之前也有兜底**：你说一声"忘了密码"，我在服务器上跑一条命令给你换新密码（这条路径已实测可用，含真实登录验收）。

### 关于"以后小团队用上"

现在这套已经能支撑了：店主账号 = 你的邮箱；新成员各自用自己的邮箱建账号（我一条命令，1 分钟）；密码忘记全部走邮件自助。唯一要记住的规矩还是那条：**在"一个库只能激活一个 release"这个结构性问题解决前，先别往后台录真实商品/客户数据**（见 §2 与 `docs/REMAINING_WORK.md` 的 P0）。

> ✅ **2026-09-18 更新：这条规矩已经解除了——"升级路径"写完了。** 之前每次发版都只能在**空库**上迁移，也就是"发一次版清一次库"；现在新增了一条**升级证据路径**：发版时先在改动前存一份加密备份、并留下"迁移前后逐表行数"的见证，然后才动 schema。**下一次发版开始生效**（就是最近这一次），此后发版不再需要清库，所以你录进后台的真实商品/客户数据不会再被发版删掉。
>
> 我核对时发现库已经不是空的了：**155 个关系 / 147 张表 / 601 行**（你的店主账号就在里面）。**如果没先写这条升级路径就发版，这些会被一起删掉。** 所以这周这件事比原计划更急，现在它排在最前面。
>
> **你现在不需要做任何事**：升级流程全在我这边跑，跑完我会给你一份"发版前后对照"（迁移了哪些表、行数有没有变化、改动前的恢复点在哪）。唯一要提醒的是：**在没有跑完这次升级发版之前，先别急着录真实商品数据**——不是因为会被删，而是因为这次发版本身要让库短暂停几分钟。

---

## 1. 告警通道 — ⚠️ 飞书 ✅ / Slack ❌（2026-09-18 重新实测）

**2026-09-18 最新实测**（发版后按规矩复跑的真实投递验证：故意制造一次故障，逼出一次真告警 + 一次真恢复）：

```
INFO  alert channel feishu accepted the payload                       ← 飞书：通了
ERROR alert channel slack did not accept the payload (status 404 …)   ← Slack：这个地址已经不存在了
WARN  alert reached 1/2 channels; no acknowledgement from: slack
ERROR monitoring failed 1/12 checks: tls_certificate                  ← 故意造的故障

INFO  alert channel feishu accepted the payload                       ← 恢复通知
ERROR alert channel slack did not accept the payload (status 404 …)
INFO  monitoring passed 12/12 checks
```

**怎么读这个结果**：

- **飞书这条已恢复正常**（之前一直回 `19024` 拒收；你把过滤方式换成 IP 白名单之后就通了）——**你现在收得到告警**，这是实测结论，不是推测。
- **Slack 这条断了，而且断得很干净**：`404` 的意思是"这个 webhook 地址在 Slack 那边不存在了"（应用被卸载、webhook 被删或重新生成过）。不是网络问题、不是签名问题，**是地址本身作废了**。
- 监控是**两条通道同时发、任一条被确认即算送达**——所以现在是"1/2 通道"：**告警仍然能到你手上**，只是少了一层冗余。这正是当初做双通道的目的。

**为什么这件事只能你来**：`404` 说明旧地址已被作废，**我这边改代码救不回来**，只能由你在 Slack 里重新生成一个 Incoming Webhook URL 给我（§1.6，1 分钟；URL 不会进聊天）。

> §1.1–§1.3 是更早三次关键词漂移的**诊断记录**（其中"Slack 那条一直通"是当时的事实，**已被今天这次实测推翻**）。留作参考：**万一某条通道又哑了，按那里的顺序定性，不要凭猜**——先看日志里的厂商错误码。

### 1.1 为什么飞书这条又哑了（以及为什么我不想再"适配"下去）

你的飞书机器人开了**"自定义关键词"**过滤：只有正文里**逐字包含**你设的那个词，飞书才收，否则**静默拒收**——而且拒收时返回的是 **HTTP 200**，光看状态码会以为发成功了。

问题在于**这个词由群侧掌握，改动不会通知主机**。今天一天里它变了三次：

| 时间 | 你设的关键词 | 我们的消息前缀 | 结果 |
| --- | --- | --- | --- |
| 上午 | `[PawShop 告警]` | `[PawShop 恢复]` / `[PawShop 告警]` | 告警能到、恢复到不了（不对称） |
| 下午 | `[PawShop]` | `[PawShop 告警]` | **全部被拒**（`[PawShop 告警]` 里不含 `[PawShop]`） |
| 傍晚（我改成 `[PawShop] 告警` 后） | 又变了（不是 `[PawShop]`、也不是 `PawShop`） | `[PawShop] 告警` | **仍被拒** |

我随后用只读探针枚举了 18 个候选（`PawShop`、`[PawShop]`、`告警`、`监控`、`服务器`、`pawlivora` …）——**全部被拒**。而两小时前 `[PawShop]` 还是通的。

> **结论：让代码去追一个随时会变的群侧关键词，是治不好的。** 每变一次，告警就静默消失一次，而监控照样 12/12 全绿。

### 1.2 ✅ 已完成：过滤方式换成 IP 白名单

**2026-09-18 你已重建机器人，飞书不再拒收**（实测见 §1 顶部）。下面是当时那一步的做法，留作参考：

在飞书群里点：`...` → **设置** → **群机器人** → 点开你的机器人 → **安全设置** → 选 **IP 白名单**，填：

```
47.254.26.124
```

（这是生产主机的真实出口公网 IP，我已在主机上实测确认。）

**为什么这个更好**：① 与消息正文彻底解耦——以后我改告警文案、改前缀都不会再有风险；② 比关键词更安全——只有这台主机能往群里发；③ 一次设定，长期有效。

改完**跟我说一声**，我 1 分钟复跑一次真实投递验证，两条消息（告警 + 恢复）会落到你群里。

**如果你更想保留关键词**：那就把它设成 `PawShop`（或 `告警`）——我们所有消息都以 `[PawShop] 告警` 开头，必然包含这两个词；然后**也告诉我一声**，我复跑验证。代价是它以后还可能再被改掉，而你不会收到任何提示。

> 不论选哪个：**改完请说一句**。群里的设置变更不会通知主机，"通道哑了"在日志里只是一条静默失败。

### 1.3 投递验证的证据（不是"跑通了"，是真送达）

今晚这一次（release 上线后）：

```
INFO  alert channels configured: feishu, slack
ERROR alert channel feishu did not accept the payload (status 200, provider code 19024: provider rejected
      the message: this bot requires a keyword and the alert text does not contain it; the text always
      opens with "[PawShop] 告警", so make one of its words a keyword on the bot or drop the requirement)
INFO  alert channel slack accepted the payload          ← Slack 真收了
WARN  alert reached 1/2 channels; no acknowledgement from: feishu
```

注意那条 `ERROR`：**它现在自己就把原因和修法写在日志里了**（这是今晚顺带补的短板——以前只说"没接受"，不说为什么）。以后这类问题看一眼日志即可定性，不用再临时写探针。验证用的状态文件与生产状态文件是分开的，跑完即删。

### 1.4 以后怎么改、怎么复跑

- 改通道：编辑 `/etc/pawshop-monitor/monitoring.env` 里的 `PAWSHOP_MONITOR_ALERT_CHANNELS`（`provider:URL` 逗号分隔，支持同时多条）。
- 复跑投递验证：`ops/commerce/run-monitor-verification.sh`（用法见 `docs/RUNBOOK.md` §9.3）；约 1 分钟，会在群里留一条明显的自检消息。
- 想再加一个通道（比如以后国内可用的 Telegram 替代品）：告诉我，我加一条即可。
- ⛔ **不要**在飞书里勾"签名校验"——加签我还没实现，勾了会全部被拒（想开就说，我补上再加）。
- 🔑 若坚持用关键词：按飞书官方规则，只有当"能当 `[PawShop] 告警` 的子串"才应该通——`[PawShop]` ✓、`告警` ✓、`PawShop` ✓；`PawShop 告警`（中间带空格）✗、`【PawShop】`（全角括号）✗。**但注意：2026-09-17 傍晚实测连 `[PawShop]` 本身都被拒了（18 个候选全部被拒）**，说明你那个群现在的过滤方式**并不是**"自定义关键词"。所以这条规则只是理论参考，**别再靠它排查**——直接按 §1.2 把过滤方式换成 IP 白名单。

### 1.5 🔄 轮换 webhook：把泄漏过的地址作废（2026-09-17 新增）

**为什么要做这一次**：webhook URL 等同于一把"只能往你那个群发消息"的钥匙。在调试飞书那两次拒收时，我为了看配置**把这条 URL 打印进过会话记录**。虽然它权限很小（不能读你的消息、不能动你的账号，删掉机器人即失效），但"进过聊天记录"本身就够格轮换了——轮换的逻辑不是"已经被滥用"，而是"它不再只存在于那一个安全边界内"。

**顺手把飞书的两件事一次做完**：轮换飞书机器人 = 删旧机器人 + 建新机器人，而"安全设置"就在建机器人的同一步里。所以**这一次轮换请直接把过滤方式选成 IP 白名单**（§1.2），不要重建一个同样用关键词的——否则你等于把"随时会再哑"这个毛病又装了回去。

#### 飞书（建议连 IP 白名单一起改）

1. 打开飞书 → 进入接收告警的那个群。
2. 右上角 `...` → **设置** → **群机器人** → 找到现有的 `PawShop 告警` → **移除**。
   （移除后旧 Webhook 地址立刻失效——这正是轮换的目的。）
3. 同一页面 → **添加机器人** → **自定义机器人** → 名字仍叫 `PawShop 告警` → **添加**。
4. **安全设置**：选 **IP 白名单**，填 `47.254.26.124`（生产主机真实出口 IP，已实测确认）。⛔ 不要选"自定义关键词"，也不要勾"签名校验"。
5. 点**完成** → 复制那条 **Webhook 地址**（`https://open.feishu.cn/open-apis/bot/v2/hook/...` 一长串）。

#### Slack

官方入口：<https://api.slack.com/apps> → 打开你建的那个 App（`PawShop Alerts`）。

1. 左侧 **Incoming Webhooks** → 在 **Webhook URLs for Your Workspace** 列表里，把旧的那条点 **Remove**（或点 **Regenerate** 换新）。
2. 点 **Add New Webhook to Workspace** → 选接收频道 → **Authorize**。
3. 复制新生成的 URL（`https://hooks.slack.com/services/...`）。

#### 新地址怎么给我（三选一，都不会让 URL 进聊天）

1. **剪贴板直取（最省事）**：你点"复制"后跟我说一句"复制好了"。我在你本机执行 `pbpaste` 读出，**读完立刻清空剪贴板**——URL 不落地、不进聊天记录。
2. **本地文件**：存成 `~/.pawshop/alert-webhook.url`（一行，只有 URL 本身），说一句"存好了"。我读走后**删除该文件**。
3. **你自己在服务器上写**（想完全绕开我也行）：登录主机后交互式编辑，URL 不会进 shell 历史 ——
   ```bash
   nano /etc/pawshop-monitor/monitoring.env   # 改 PAWSHOP_MONITOR_ALERT_CHANNELS 那一行
   ```
   写完保存即可（文件权限保持 `root:pawshop 0640`，不要放宽）。

#### 我接手后的收尾（约 1 分钟，不需要你操作）

1. 更新 `/etc/pawshop-monitor/monitoring.env` 里的 `PAWSHOP_MONITOR_ALERT_CHANNELS`（**同时保留 feishu 与 slack 两条**，一条死了还有另一条兜底）。
2. 复跑真实投递验证：`ops/commerce/run-monitor-verification.sh`（会各造一次"故障"和"恢复"）。
3. **判定标准**：日志里每条通道各一行 `accepted the payload`，**并且**你能在飞书群和 Slack 频道里真的看到那两条自检消息。缺一都不算通过——我不接受"日志说通了"。

> 验证用的状态文件与生产文件是分开的，跑完即删；告警状态不会被这次验证污染。

---

### 1.6 🔧 需要你做的：重新生成 Slack webhook URL（2026-09-18 新增）

1. 打开 <https://api.slack.com/apps>，选中你之前建的那个应用（没有的话新建一个）。
2. 左侧 **Incoming Webhooks** → 打开 **Activate Incoming Webhooks** → **Add New Webhook to Workspace** → 选告警要发到哪个频道 → **Allow**。
3. 复制生成的 `https://hooks.slack.com/services/…` 地址。
4. 按 §1D 的三选一方式给我（**不要直接贴在聊天里**）。

我接手后会自动：备份现有配置 → 写进主机的告警配置 → **复跑一次真实投递验证（告警 + 恢复各一条）** → 确认日志里两个通道都 `accepted`，然后告诉你结果。整个过程约 2 分钟，**不需要发版、不需要重启商务服务**。

> **为什么"再贴一次同一个地址"没用（2026-09-18 补充实测）**：你给我的那个 Slack 地址，与主机上现存的**逐字相同**（指纹同为 `cb1f84d459ef`），而且**结构完整**——`/services/` 后面是标准的 `T`+10 位 / `B`+10 位 / 24 位，字符集干净、没有截断也没有多余字符。也就是说**服务器这边从来没配错**，是 Slack 那边把这条 webhook 撤销掉了。所以必须**新生成一条**来替换它。

## 1附. 附录：以后想再加一条通道时怎么点（当前不需要做）

下面是三家平台的官方入口与操作步骤，留作参考。**只有你想再加/更换通道时才需要看**。

### 1A. 飞书自定义机器人（推荐）

官方文档：<https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot>

1. 打开飞书，进入你想接收告警的群（建议**新建一个只有你的群**，例如"PawShop 运维"）。
2. 点群右上角 `...` → **设置** → **群机器人** → **添加机器人** → 选 **自定义机器人**。
3. 起个名字（如 `PawShop 告警`），点**添加**。
4. **安全设置**（重要，三个选项只选一个）：
   - ✅ **推荐：自定义关键词**，填 `[PawShop]`（推荐，等于你现在的设置）或 `告警`。**判定规则只有一条**：你填的这段文字必须是 `[PawShop] 告警` 的子串——所以 `[PawShop]` ✓、`告警` ✓、`PawShop` ✓，而 `PawShop 告警`（中间带空格）✗、`【PawShop】`（全角括号）✗。填错不会报错，只会静默丢消息。
   - 或者 **IP 白名单**，填 `47.254.26.124`（这是生产主机的真实出口公网 IP，我已在主机上实测确认）。
   - ⛔ **暂不要勾选"签名校验"**：它要求发送方用 `timestamp + "\n" + 密钥` 做 HMAC-SHA256 再 Base64，**当前监控实现还没做加签**，勾了会导致告警全部被拒。如果你希望更安全、想开加签，告诉我，我补上签名支持（含单测）后再开。
5. 点**完成**，复制那条 **Webhook 地址**，形如：
   `https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`
6. ⚠️ **别只复制"Webhook URL"这几个字**（上次就是这样：剪贴板里只有那 11 个字节的标签）。要复制**以 `https://` 开头的那一长串**。

### 1B. Slack Incoming Webhook

官方文档：<https://docs.slack.dev/messaging/sending-messages-using-incoming-webhooks/>

1. 先在 <https://api.slack.com/apps> 点 **Create New App** → **From scratch**，起名 `PawShop Alerts`，选一个 workspace（免费版即可）。
2. 左侧 **Incoming Webhooks** → 把 **Activate Incoming Webhooks** 打开。
3. 页面下方点 **Add New Webhook to Workspace** → 选择接收频道（建议建一个私有频道）→ **Authorize**。
4. 复制生成的 Webhook URL，形如：
   `https://hooks.slack.com/services/<TEAM_ID>/<BOT_ID>/<TOKEN>`

### 1C. Telegram Bot（当前无法使用：需要海外手机号）

官方文档：<https://core.telegram.org/bots/api>（BotFather 入口：<https://t.me/BotFather>）

你已经确认登录不上 Telegram，所以这条**暂时放弃**。监控已经支持它——将来如果你有了可用的账号，只要新建 bot、把 token + chat_id 给我，我加一条配置即可，不需要改代码。

### 1D. 想再加通道时，URL 怎么交给我

**不用给账号，也不用让我登录你的任何账号。** 我需要的只是那一条 webhook URL——它的权限被限制在"只能往你那个群发消息"，而且你在群里删掉机器人就立刻作废；账号权限是全部，代价完全不成比例。所以**不要把账号密码、验证码、扫码登录交给我**。

按 §1.4 的同一套交接方式给我（也已在 `docs/RUNBOOK.md` §9.3 记明）：

1. **剪贴板直取（最省事，推荐）**：你点一下"复制"，跟我说一句"复制好了"。我在你本机执行 `pbpaste` 把它读出来，**读完立刻清空剪贴板**——URL 不进聊天记录、不落地成文件。
2. **本地文件**：存成 `~/.pawshop/alert-webhook.url`（一行，只有 URL 本身），告诉我"存好了"。我读走后**删除该文件**。
3. **你 SSH 上服务器自己写**（全程不经我手）。

Webhook URL 等同于"能往你频道里发消息"的写入凭据，看到它的人就能冒充你的运维告警，所以：

- ⛔ 请不要：贴进聊天、截图、提交到 Git、放进网盘。**Slack 官方会主动扫描并吊销泄露的 webhook**。
- 万一泄露了：在群里删掉这个机器人、重新添加一个新的，旧 URL 立即失效（约 1 分钟）。

**实际交接记录（2026-09-17）**：本次用的是"桌面文件 + 我读到后立即删除源文件"的方式，两条 URL 都已接入并在主机上完成真实投递验证。

---

## 2. 备份链（P0）— 阻塞已全部清除，凭据已实测

**现状（2026-09-17，含一次实测修正）**：四处硬阻塞**全部清除**，备份专用凭据**已建立并实测通过**（能写、能精确回读、**不能删**）。首次加密备份按既有脚本执行时暴露了一个**我自己的设计漏洞**：运行时原来用 `GetBucketVersioning` 做上传前检查，而备份身份按设计读不到任何桶级信息，那个检查**永远不可能通过**。已改成**对象级证明**（每次上传必须拿到版本号 + 按精确版本回读），但这意味着**需要一个包含该修复的新 release**，首次备份才能跑通。首次加密备份 + 离线回读 + 隔离恢复演练，会在那个新 release 就绪后按既定契约执行——它需要先把提交推送到 GitHub（§5.1）。

| 阻塞 | 内容 | 状态 |
| --- | --- | --- |
| ① | 单元 `WorkingDirectory=/srv/pawshop-commerce/current/_commerce` | ✅ 首次备份流程用的是 release 自身的目录（`--property=WorkingDirectory=$release/_commerce`），不需要 `current`。**待 release 就绪。** |
| ② | `/etc/pawshop-backup/backup-offsite.env` 缺失 | ✅ **2026-09-17 已完成**：端点/区域/桶/保留期 + **两个闸门已打开**（见下）。 |
| ③ | `LoadCredential` 需要的两个凭据文件缺失 | ✅ **2026-09-17 已由我建好并实测**（用你给的临时管理 key 全自动完成，你没动过手）。 |
| ④ | 单元 `Requires=postgresql.service` | ✅ **2026-09-16 已修复** → `postgresql@17-main.service`。顺带纠正一个说法：`postgresql.service` 并不是"inactive 所以起不来"，它是个**空壳单元**（`ExecStart=/bin/true`），依赖它等于**没有任何保证**；真正的集群单元是 `postgresql@17-main.service`。 |

**两个闸门不是"填个 1"，而是实测过的事实**：

| 闸门 | 含义 | 我怎么测的 |
| --- | --- | --- |
| `PAWSHOP_BACKUP_S3_VERSIONING_CONFIRMED=1` | 桶开了版本控制，覆盖也能找回 | 用**备份凭据自己**覆盖上传同一个对象后，**旧版本仍能按版本号读回原内容**（功能性证明，比读一个状态标记更硬；2026-09-17 生产桶实测通过） |
| `PAWSHOP_BACKUP_S3_DELETE_DISABLED=1` | 备份账号**没有删备份的能力** | 用这把凭据真的发了一次 `DeleteObject` → **403 AccessDenied**；它还**读不了**生命周期规则（同样 403） |

**凭据实测记录（在生产主机上、用真实凭据跑的）**：

```
PASS 上传（PutObject）                HTTP 200 versionId=CAEQABiBgMCf…
PASS 覆盖上传（PutObject）            HTTP 200 versionId=CAEQABiBgMCr…
PASS 探测（HeadObject + 版本号）       HTTP 200 versionId=CAEQABiBgMCr…
PASS 回读明文一致（GetObject）         HTTP 200 内容匹配=true
PASS 覆盖后旧版本仍可读（版本控制）      HTTP 200 旧版本内容匹配=true
PASS 删除被拒（DeleteObject）          HTTP 403
PASS 生命周期规则不可读（越权检查）      HTTP 403
```

### 2.1 已建好的最小权限身份（供你复核，不需要你操作）

- RAM 用户：`pawshop-backup-writer`（只给程序用，**无控制台登录**）
- 自定义策略：`pawshop-backup-writer-policy`
  - 允许：`oss:PutObject` / `oss:GetObject` / `oss:GetObjectVersion`（只限 `pawlivora-backups-us-west-1/pawshop/database-backups/*`）
  - **显式拒绝**：`oss:DeleteObject`、`oss:DeleteObjectVersion`、`oss:DeleteBucket`、`oss:PutBucketLifecycle`、`oss:PutBucketVersioning`、`oss:PutBucketPolicy`、`oss:PutBucketAcl`、`oss:PutBucketReplication`
  - 实际上这个身份**连桶级信息都读不到**（`?versioning`、`?lifecycle` 都是 `403 AccessDenied`）。这是刻意的：写备份的账号不需要、也不该看到过期规则。配套地，**备份程序不读桶的版本控制状态**，而是在每次上传时要求存储返回版本号来证明版本控制已开启——`DeleteObject` 被拒 + 读不到桶级信息，这两条越权测试因此都必须是 403。
- 密钥：**恰好一把**，直接写在主机 `/etc/pawshop-backup/backup-s3-access-key`、`backup-s3-secret-key`（`root:root 0600`，只被 `pawshop-backup.service` 通过 systemd 的 `LoadCredential` 读取，**不进 Git、不进聊天、不落到别的文件**）
- 复核入口：<https://ram.console.aliyun.com/users> → `pawshop-backup-writer` → 权限管理

### 2.2 我做剩下的（等 release 就绪，不需要你动手）

①（release）→ 首次迁移 → 首次加密备份 → OSS 精确版本回读 → 隔离恢复演练（恢复到临时库、核对表数量、删除临时库）→ 记录证据到 `/var/lib/pawshop-release-evidence/<sha>` → 才启用 `pawshop-backup.timer`。

### 2.3 ⚠️ 只剩这一件要你点两下：删掉临时管理用户

**故事**：我按你同意的"方式二"建了临时用户 `pawshop-agent-temp`，用它完成了上面全部工作。收尾时我犯了一个顺序错误——**先解除了它的策略，才去删它的密钥**，结果我在解除策略的同一刻就失去了 RAM 管理权限，**删不掉自己这个用户了**。

**好消息是它已经彻底作废**。我用它实测过，现在三个面全是拒绝：

```
OSS 管理面 ListBuckets        -> HTTP 403 AccessDenied
OSS 数据面 PutObject          -> HTTP 403 AccessDenied
RAM 管理面 ListUsers          -> HTTP 403 NoPermission
```

**请你做（约 1 分钟）**：

1. 打开 <https://ram.console.aliyun.com/users>
2. 找到 **`pawshop-agent-temp`** → 右侧 **删除**
3. 若提示"该用户存在 AccessKey，无法删除"：先点进去 **AccessKey → 删除**（它已无任何权限，删除是纯清理），再回到用户列表删除用户
4. 顺便确认用户列表里只剩 `pawshop-backup-writer` 和 `pawshop-media-runtime`

> 这一条**不在关键路径上**（那把 key 已经什么都做不了），你有空再点都行。

---

## 3. OSS 生命周期规则 — ✅ 已配置并逐条核对

控制台复核入口：<https://oss.console.aliyun.com> → 选 `pawlivora-backups-us-west-1` → **数据管理 → 生命周期**

**我已写入并回读核对的规则（共 4 条）**：

| 规则 ID | 前缀 | 动作 |
| --- | --- | --- |
| （原有规则，保留） | 全桶 | 非当前版本 3 天后清理 |
| `pawshop-daily-90d` | `pawshop/database-backups/daily/` | 保留 90 天后删除 |
| `pawshop-monthly-365d` | `pawshop/database-backups/monthly/` | 保留 365 天后删除 |
| `pawshop-yearly-1095d` | `pawshop/database-backups/yearly/` | 保留 1095 天后删除 |

三个数字与代码里的常量一致（`_commerce/scripts/offsite-backup-policy.cjs` 的 `RETENTION_DAYS = { daily: 90, monthly: 365, yearly: 1095 }`），所以规则和备份链不可能各自漂移。

**两件在操作中必须说明的事**：

1. **⛔ 没有创建覆盖整桶的"90 天删除"规则。** 三条规则各自只匹配自己的前缀，月度和年度归档不会被日常规则清掉。
2. **我改动了原有的那条全桶规则——只删掉了它的一个元素，原因如下。** OSS 不允许两条前缀重叠的规则有**同一种动作类型**，写入时报 `InvalidRequest: Overlap for same action type Expiration`。原规则带一个 `Expiration`（清理失效删除标记），与我的三条前缀规则冲突，所以我把那个元素去掉、保留了它真正的用途（**非当前版本 3 天后清理**，规则 ID 与天数都没动）。**副作用**：以后被生命周期删掉的当前版本留下的"删除标记"（零字节元数据）不会自动清理，不影响数据、不影响费用。我给你留了原文件备份 `/root/pawshop-offsite.env.bak-20260917` 与当前配置导出，需要时随时可回滚成原样。
3. **保留期是你的合规判断**：现在是 90 天 / 12 月 / 3 年。若财税或平台对某些类目要求更长（例如 5 年），告诉我，我改规则或改代码常量（两边一起改，保持不漂移）。

> ⚠️ 已实测：主机上那把**给商务运行时用的 OSS key 管不了这些**——它连 `ListBuckets` 和读 `?lifecycle` 都被拒（`403 AccessDenied`），说明它是严格按对象前缀收窄的。所以生命周期规则确实需要另一把凭据，不是我没找对入口；现在建好的那把也一样**读不了、改不了**规则（这是故意的：写备份的账号不该能改"什么时候删备份"的规则）。

---

## 4. 收款通道

| 项 | 归属 | 说明 |
| --- | --- | --- |
| 申请空中云汇 **Airwallex**（第一生产候选） | 只有你 | 官网：<https://www.airwallex.com.cn> |
| 同步申请 **PingPong**（费率对照 + 备用通道） | 只有你 | 官网：<https://www.pingpongx.com> |
| 两家通过后**只接一家到生产**，另一家保留 | 你决定，我接线 | |
| **先沙盒测试，不开放真实扣款** | 共同 | |

**如实填写的纪律（重要，填错会被风控冻结）**

- 主体：**个体工商户**（⚠️ 营业执照下证后再提交，不要先填后补）
- 网站：`pawlivora.com`；业务：自营宠物用品独立站；首要市场：美国；发货地：中国大陆
- 客单价与预计月销售额按**冷启动的真实低额**填写
- ⛔ 不要填"已有大量订单""已有海外仓""已确定送达时效"，除非已经落实
- ⛔ **身份信息、银行卡、营业执照、短信验证码、最终协议一律你本人提交**；不要把密码、验证码、完整证件发到聊天里（打码截图可以问我）
- 参考：Airwallex 通常要求营业执照 + 法人身份 + 业务流水，审核门槛比纯平台收款工具高；PingPong 对中国卖家流程更轻。**两家并行申请互不阻塞**，这是稳妥做法。

**通道通过后我负责**：把沙盒凭据落到服务器（不进 Git）、接支付插件、跑通完整测试清单（成功 / 失败 / 取消 / **重复 webhook** / 退款 / 对账）。

---

## 5. 仓库与 CI

### 5.1 ✅ 已推送并已发版（2026-09-18 结项）

**2026-09-18 你说了可以推送，我把本轮改动推上去，并一次跑完了整条链**：主机取码 → 构建不可变 release `aaa5673` → **升级迁移（不清库）** → 加密备份 + 异地精确版本回读 + 隔离恢复演练 → 合闸 → 激活 → 验收通过。库里的 **147 张表 / 601 行一行未少**，你的后台账号还在。

下面是当时"为什么必须推送"的说明，留作记录：

**当时有两件事被"没推送"卡着**：

1. **激活商务后台走不通**。顺序是设计强制的：激活 → 必须先做一次"首次迁移 + 首次加密备份 + 隔离恢复演练"。而这三步都要求**主机源码树正好停在那个 release 的提交上**（`git rev-parse HEAD` 必须等于 release ID，且工作树干净）。主机的 `git fetch` 是**匿名**的，只能拿到**已推送到 GitHub 的提交**。
2. 顺带一提：主机上现成的那个 release（`79a045c`）**用不了**——它里面**没有** `write-production-backup-restore-evidence.mjs` 这个文件（那是后来才加的），所以走不了首次备份的证据流程。

所以我需要你说一句"**可以推送**"，我把本轮提交推上去，然后一次跑完：主机取代码 → 准备 release → 首次迁移 → 首次加密备份 + 离线回读 + 隔离恢复演练 → 激活商务后台。

**提交内容你可以在推之前先看**：都是本轮的工程改动与文档（监控多通道路由、告警报文信封修正、备份凭据与生命周期、`run-monitor-verification.sh`、文档同步），没有密钥、没有客户数据、没有真实 webhook URL（我做过敏感词自检）。要看清单我随时给你 `git log --stat`。

> 你原来的规矩是"要推的先问你"，所以我停在这里等你点头，**没有擅自推送**。

| 项 | 我的处理 |
| --- | --- |
| `main` 与工作分支分叉 | **建议商务后台激活跑通后再合并**，避免一次涌入过多变更。你说合我再合。 |
| CI 只在 `main` 触发 | 可选：把 workflow 扩到工作分支 |
| GitHub Pages | 已于 2026-09-16 停用（返回 404）。如需重开：<https://github.com/wesley9311/pawshop/settings/pages> |
| 仓库可见性 | 目前 **PUBLIC**。⚠️ 转私有前必须先给主机配 deploy token——主机的 `git fetch` 是**匿名**的，**转私有会立刻打断发布链**。 |
| 历史提交含 `costCNY`（AR-14） | **已决策：保持公开、不重写历史**。泄露范围只是历史提交里的采购成本，不是客户数据或凭据；`HEAD` 的 `catalog.json` 已干净，`check-security.mjs` 已把它列为禁止字段。 |

---

## 6. 后台暴露方式（B2）—— 需要你一句话

现状：Medusa 后台按设计只在**回环**上跑（`127.0.0.1:9000`，不对外）。这最安全，但**浏览器里的中文运营台够不到它**（你的浏览器在你电脑上，访问不到服务器的回环口）。三条路：

| 方案 | 说明 | 代价 |
| --- | --- | --- |
| (a) 保持回环 + SSH 隧道 | 运营台只在你本机跑 | 最安全，但"随时随地用"很别扭 |
| **(b) 同源反向代理（推荐）** | nginx 在 `pawlivora.com` 下开 `/admin-api/` → `127.0.0.1:9000`，叠加 IP 白名单或 Basic Auth，再叠 Medusa 自身鉴权 | 攻击面从 0 变 1，但可控；**运营台才能用** |
| (c) 直接公网暴露 Admin API | 最方便 | ⛔ 不推荐 |

**我的判断：走 (b)。** 这个决定必须在运营台开工前定，否则做完才发现调不通。你回一句"按 (b) 走"，我就往下执行；如果你更在意"绝不对公网开任何口"，那就走 (a)，运营台改成只在你本机运行。

---

## 7. 本轮（2026-09-17）我已自行完成、不需要你操作的

**告警（P0，已修复、待随下次发版生效）**

- **双通道接入并真实投递验证**：飞书 + Slack 同时投递，任一条被对方确认即算送达；缺一条会在日志里点名，不会躲在另一条后面。
- **发现并修掉一个只有实测才能发现的缺陷**：飞书自定义关键词过滤按**正文子串**匹配，未命中即拒收，而拒收返回 **HTTP 200 + `code:19024`**（光看状态码会当成发成功）。原来的"恢复通知"以 `[PawShop 恢复]` 开头 → 会造成"出事收到告警、恢复永远收不到"。现已让**所有消息共用同一个前缀**，并加回归测试锁死。
- ⚠️ **同日晚修正**：这条"已验证送达"的结论**当天晚上就被推翻**——生产真的触发一次告警时飞书回 `19024`，逐候选探测发现该群关键词已从 `[PawShop 告警]` 变成 `[PawShop]`，于是**飞书通道自那时起静默丢弃全部告警**，只有 Slack 在响，而监控一直 12/12 全绿。**群侧改设置不会通知主机，这就是它没被发现的原因。**
- **修法**：信封改为 `[PawShop] 告警`（同时含 `[PawShop]` 与 `告警` 两种写法，你现有的关键词直接命中，**不需要你改任何设置**）；日志补上厂商错误码与含义（下次一眼看出该改哪里）；端到端夹具增加"关键词拒收"用例。**已用真实飞书机器人与真实 Slack webhook 复验通过**（告警 + 恢复各一条，四个渠道全部 `accepted`）。
- ⏳ **仍待在下一个 release 里上线**（只涉及监控的 2 个文件，**不需要重新激活商务、不需要清库**）。上线前飞书那条通道仍是哑的，**Slack 是当前唯一可靠的告警出口**——这也是把它放在 P0 的原因。
- 告警端到端由 10 项扩到 **16 项**（4 个多通道场景 + 冲突配置拒绝启动 + 关键词拒收日志），全绿。

**备份链（P0，阻塞已清）**

- 建好最小权限身份 `pawshop-backup-writer` + 策略 `pawshop-backup-writer-policy`，密钥**恰好一把**、直接写进主机 `root:root 0600` 文件，只在 `pawshop-backup.service` 里通过 `LoadCredential` 读取。
- **用真实凭据实测**：能写、能精确回读版本、**`DeleteObject` 被拒 403**、读不了生命周期规则（越权检查也 403）。
- `/etc/pawshop-backup/backup-offsite.env` 的**两个闸门已打开**，且是"测过才开"而不是"填个 1"。
- **OSS 生命周期三条规则已写入并回读核对**（90/365/1095 天），与代码常量一致；原有全桶规则予以保留（详见 §3 的说明）。

**监控与运维**

- 监控脚本升级为多通道版并**已装到生产主机**（`/usr/local/libexec/pawshop`，`root:root 0555`，装前备份、装后 `cmp` 核对、升级前后各跑一次确认 12/12 无回归）。
- 新增 `ops/commerce/run-monitor-verification.sh`：**用与 systemd 单元完全相同的身份和环境**跑监控，额外的覆盖项放在 root-only 文件里——这样验证时 webhook URL 不进命令行、不进 shell 历史。

## 8. 建议顺序（与 `docs/REMAINING_WORK.md` §F 一致）

1. **你回一句后台暴露方案**（§6）— 一句话
2. **你说"可以推送"**（§5.1）← **当前唯一的关键路径阻塞**
3. 我跑完：主机取码 → release → 首次迁移 → 首次加密备份 + 离线回读 + 隔离恢复演练 → **激活商务后台** → 删掉监控里那两行临时跳过（监控变成 12/12 全真实检查）→ 启用备份定时器
4. 我开工**中文运营台**（从只读看板起步）
5. 你有空时删掉临时用户（§2.3，1 分钟，不在关键路径）
6. **你申请收款**（§4）→ 通过后我接沙盒

每一步之后我都会跑一遍全量回归（根检查 + commerce 测试/类型/构建 + 真实浏览器检查 + `verify:production` 双档 + 监控实测 + 告警端到端）。

每一步之后我都会跑一遍全量回归（根检查 + commerce 测试/类型/构建 + 真实浏览器检查 + `verify:production` 双档 + 监控实测）。
